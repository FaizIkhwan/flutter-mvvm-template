name: mvvm_template
description: Faiz's MVVM Template

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.3
  get_it: ^6.0.0
  provider: ^5.0.0
  modal_progress_hud: ^0.1.3
  intl: ^0.17.0
  sqflite: ^2.0.0+2
  http: ^0.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
